# ===========================================================
# ANIMA · Plataforma — variables de entorno
# Copia este archivo como .env.local y rellena los valores.
# .env.local NUNCA se versiona.
# ===========================================================

# --- PÚBLICAS (viajan al navegador; protegidas por RLS) ---
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_PLATFORM_DOMAIN=localhost:5180

# --- PRIVADAS (SOLO servidor / Edge Functions / CI) --------
# Nunca con prefijo VITE_. Nunca en este repositorio con valor.
# SUPABASE_SERVICE_ROLE_KEY=
# SUPABASE_DB_URL=
# PAYMENT_WEBHOOK_SECRET=
# MERCADOPAGO_ACCESS_TOKEN=
# TRANSBANK_API_KEY=
