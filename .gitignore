# Local secrets and credentials
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*secret*
*token*
*askpass*

# Local tooling noise
.DS_Store
node_modules/
.cache/
.tmp/
tmp/
dist/
build/
coverage/

# Supabase local development
supabase/.branches/
supabase/.temp/
supabase/.env
supabase/.env.*
.claude/

# Reforzado 2026-08-22 (plataforma)
logs/
*.log
.env.local
*.bak
backups/

# Artefactos de compilación
*.tsbuildinfo

# Datos de clientes (07-09-2026)
# Este repositorio es PÚBLICO y se publica por GitHub Pages. Las cargas de
# datos reales —valoraciones, EBITDA, rondas, nombres de personas— viven
# aquí y NUNCA se versionan. El sitio donde esos datos pertenecen es la
# base de Supabase, que es privada.
supabase/datos-clientes/
